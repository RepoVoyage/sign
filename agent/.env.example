# 填写兼容 Chat Completions 协议的模型服务配置。
# BASE_URL 填 API 前缀（通常包含 /v1），不要填写 /chat/completions。
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
LLM_TIMEOUT_SECONDS=10
# 部署到公网时必须设置独立的访问令牌，客户端通过 Authorization: Bearer 传入。
SERVICE_API_KEY=
