# 边界能力验证报告（契约§7 门槛项，2026-09-23）

方法：离线模拟连续流（`continuous_stream.py`）——100 段缓存关键点序列拼成 4 个会话、
clip 间插 1.5s 静止；腕速状态机分割（<0.015 肩宽单位/帧 持续 0.4s），低 margin 邻段合并，
段终点回收到最后运动帧。非固定静默时长、非 LLM 标点。

| 指标 | 结果 | 门槛对照 |
| --- | --- | --- |
| 段检出 | 12/12（0 漏切） | §7 误切/漏切率随模型交付 → 本模拟 0/12 |
| 误报段 | 0 | 同上 |
| 边界误差 | 起点 MAE 0.00s / 终点 MAE 0.13s | — |
| 整句识别（段级） | 12/12 | — |
| 草稿+边界延迟 | 识别侧实时率 2.8x；pose 64–76ms/帧 → 边界事件在停顿证据齐备后 <300ms 可达 | §5 ≤300ms：满足（模拟） |
| 实时 HTTP 服务验收 | 17 句 ×1 段 17/17（`service_acceptance_20260923.json`） | — |
| WSS 契约 loopback | H.264 原流推流 26 事件，终句正确、tokenSpans 齐备 | §4/§5 连通 |

**局限（发布前须知）**：
1. 停顿为模拟插入的静止段；真人句间停顿分布未实测，误切/漏切率以真机联调复测为准。
2. 30 分钟持续流压测**未做**（§7 门槛），列为联调联合测试项。
3. 模板库来自 Tr1zw 100 段（第三人称、4K/540p）；GO 3S 640×384 鱼眼为新域，
   准确率不继承本报告数字——**要求 App 侧先供 2–3 分钟真机样流做域校验**。
