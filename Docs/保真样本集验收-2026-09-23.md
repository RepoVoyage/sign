# 保真样本集验收记录 v1（2026-09-23）

> P7 验收行「否定/数字保真样本集」的交付记录。
> 方法：真机 training 包（DeepSeek 云端 LLM 凭据已配置），adb 自动化点击
> 「开始翻译」跑桩脚本两轮（含 4 句保真样本），停止后拉取 Room 库离线复核。
> 复核规则与 `DirectLlmPolisher.guardResult` 一致：否定词（不/没/无/别/未）
> 计数 + 数字（阿拉伯与中文数字）计数，原文 vs 模型整理文本。

## 跑批窗口

16:33:23 ~ 16:35:09（约 106s），zh-CN 结果 **21 条：READY 20 / UNAVAILABLE 1**。

## 保真样本逐条（8 次出现）

| 原文 | 次数 | 状态 | 否定保留 | 数字保留 | 结论 |
|---|---|---|---|---|---|
| 请不要碰我的头 | 3 | READY | ✅ | ✅（无数字） | 保真 |
| 我有3个孩子 | 2 | READY | ✅ | ✅（"3" 原样保留） | 保真 |
| 药每次吃2片 | 2 | READY | ✅ | ✅（"2" 原样保留） | 保真 |
| 我没说明天去 | 1 | READY | ✅ | ✅ | 保真 |
| 我没说明天去 | 1 | UNAVAILABLE | —（无文本） | — | 单次调用失败，非保真问题 |

- **保真率：8/8 有文本样本全部保留否定与数字**；模型未做数字写法归一
  （"3" 未变 "三"），guard 的数字计数规则未误触发。
- **guard 一致性：8/8**——保真样本全部 READY（无虚假待核对）；窗口内
  LLM 侧 NEEDS_CONFIRMATION = 0，故本轮无震动事件（与震动策略相符）。
- UNAVAILABLE 1 次：单次云端调用失败（网络/限流类），§6 错误矩阵正确降级为
  「不可用」，不伪装译文。

## 震动策略交叉核对

- 窗口内识别侧待核对卡片（桩脚本 UNCERTAIN 句「可能是低电量」）出现多次，
  **均不震动**——符合 2026-09-23 用户决定：唯一震动源 = LLM 结果
  NEEDS_CONFIRMATION（ARCHITECTURE §2.7 修订表）。
- 是否让识别侧待核对也震动：**待定决策**（一行接线），见 plan.md 待办。

## 结论与后续

- **P7 保真样本集验收：通过**（样本集 v1 = 4 句；否定/数字语义零丢失，
  guard 零误报，失败路径正确降级）。
- 后续：发布前扩充样本集（专名/有意重复/多数字句）；UNAVAILABLE 单次失败
  若在验收期复现需查云端限流；识别侧待核对震动与否待用户决策。
